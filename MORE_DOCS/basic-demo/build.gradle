plugins {
    id 'base'
}

// Now add a task that creates a zip archive from the src directory.
task zip(type: Zip) {
    from 'src'
}

task copy(type: Copy) {
    from 'src'
    into 'dest'
}

description = 'A trivial Gradle build'
version = '1.0'
